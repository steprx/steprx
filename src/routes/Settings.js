import { Box, Typography } from "@mui/material";

const Settings = () => {
  return (
    <Box p={2}>
      <Typography variant="h4" align="center">
        Settings
      </Typography>
    </Box>
  );
};

export default Settings;
